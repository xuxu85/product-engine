[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "product-engine"
version = "0.1.0"
description = "Minimal decision and orchestration core for consumer product discovery and execution."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
